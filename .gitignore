.env
.DS_Store
.venv/
__pycache__/
.pytest_cache/
.playwright-mcp/
*.pyc
.cache/
cache/
hf-cache/
model-cache/
*.bin
*.gguf
*.pt
*.pth
*.safetensors
feature_notebook.json
RESULTS.local.json
BLOCKED.local.md
artifacts/
reports/*.json
reports/*.jsonl
recipes/*/benchmark_results.json
recipes/*/candidate_features.json
recipes/*/examples.jsonl
recipes/*/sweep_results.json
# Top-level research-trail + runtime stores only (anchored so we don't ignore
# the qwen_scope_lab/experiments package or data/experiments corpora).
/experiments/
/monitors/

# Internal / local-only docs (kept on disk, not published)
HANDOFF.md
AGENTS.md
RESULTS.md
UAT_REPORT.md
docs/AGENT_HANDOFF.md
/ballast-product-pitch.html
reports/product-brainstorm.md
reports/codex-product-inventions.md
reports/lab-capability-products.md
reports/manifold-research-directions.md

# Workflow-orchestration artifacts (local run journals, not part of the lab)
/*.workflow.js
/*.workflow.run.*
/.workflow-journal/

# In-the-wild jailbreak corpora: rebuilt deterministically (seed 0) by
# scripts/jailbreak_wild_build_corpus.py from public HF datasets; the scraped
# prompt texts themselves are not republished here.
data/jailbreak_wild_raw/
data/experiments/jailbreak_wild/

# Heavy run by-products inside published evidence dirs (retrainable / re-derivable)
reports/*.zip
reports/**/*.zip
reports/sae_audit/*.npy

# Stray HF cache from a misquoted HF_HOME (junk, kept out of git)
/~/

# Local Claude Code settings + agent skills (internal tooling, not part of the public lab)
.claude/

# Defensive build/cache ignores
*.egg-info/
build/
dist/
.mypy_cache/
.ruff_cache/
node_modules/
